# AGENTS.md

## Repo Notes

- Git workflow for this repo: work directly on `main`.
- Do not create feature branches.
- Do not open or rely on pull requests.
- Do not use merge-based flow for normal changes.
- After the user approves changes, commit on `main` and push `origin main`.
